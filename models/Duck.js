export default class Duck {
    constructor(name, color, wingSpan, billSize, type) {
        this.name = name;
        this.color = color;
        this.wingSpan = wingSpan;
        this.billSize = billSize;
        this.type = type
    }
}