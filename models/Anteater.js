export default class Anteater {
    constructor(name, color, weight, snoutSize, type) {
        this.name = name;
        this.color = color;
        this.weight = weight;
        this.snoutSize = snoutSize;
        this.type = type
    }
}